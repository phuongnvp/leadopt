from __future__ import annotations


def example_mpo_score(smiles: str) -> float:
    """
    Placeholder deterministic MPO-like score.

    Replace with a real multi-objective score later.
    """
    return float(len(smiles)) * 0.1
