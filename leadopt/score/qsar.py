from __future__ import annotations


def example_qsar_score(smiles: str) -> float:
    """
    Placeholder deterministic score.

    Replace with your QSAR model inference later.
    """
    return float(len(smiles))
